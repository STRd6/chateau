- RoomList = require "./room-list"

chateau
  viewport
    = @canvas
    word-area
      = @words
  actions
    form(submit=@saySubmit)
      input
      button Speak
    button Settings

  = RoomList this
